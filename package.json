{
  "name": "forge",
  "version": "0.1.0",
  "description": "Local dev orchestration daemon",
  "main": "src/daemon/server.js",
  "bin": {
    "forge": "./bin/forge.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/daemon/server.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "jest": {
    "testMatch": [
      "**/test/**/*.test.js"
    ]
  },
  "private": true,
  "license": "MIT",
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.0.0",
    "dockerode": "^5.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^7.0.0"
  }
}
